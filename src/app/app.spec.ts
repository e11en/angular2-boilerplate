/*
 * An example test for the app component and the app module.
 * These two are combined because the component of app is
 * still really small.
 */

import {} from 'jasmine';

describe('1st tests', () => {
    // TODO: Fill this test
    it('true is true', () => expect(true).toBe(true));
});
